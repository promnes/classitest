# =============================================================================
# Classify - Production Environment Configuration for Hostinger VPS
# =============================================================================
# 
# Instructions:
#   1. Copy this file: cp .env.production.example .env
#   2. Replace ALL placeholder values with your actual production values
#   3. Generate secure secrets: openssl rand -base64 64
#   4. Run: docker compose up -d
#
# IMPORTANT: All variables marked [REQUIRED] must be set before deployment
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (PostgreSQL) [REQUIRED]
# -----------------------------------------------------------------------------
POSTGRES_USER=classify_user
POSTGRES_PASSWORD=CHANGE_TO_SECURE_PASSWORD_32_CHARS_MIN
POSTGRES_DB=classify_db

# -----------------------------------------------------------------------------
# SECURITY [REQUIRED] - Generate with: openssl rand -base64 64
# -----------------------------------------------------------------------------
# WARNING: Do not use defaults! Generate strong random values
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_STRING_USE_OPENSSL_RAND
SESSION_SECRET=REPLACE_WITH_ANOTHER_64_CHAR_RANDOM_STRING

# -----------------------------------------------------------------------------
# ADMIN ACCOUNT [REQUIRED]
# -----------------------------------------------------------------------------
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=CHANGE_THIS_MIN_16_CHARS_MIXED

# Optional: Password for /admin/env/dashboard access
ADMIN_PANEL_PASSWORD=OPTIONAL_DASHBOARD_PASSWORD

# -----------------------------------------------------------------------------
# APPLICATION URLs
# -----------------------------------------------------------------------------
APP_URL=https://yourdomain.com
FRONTEND_URL=https://yourdomain.com
BACKEND_URL=https://yourdomain.com
CORS_ORIGIN=https://yourdomain.com

# -----------------------------------------------------------------------------
# EMAIL (SMTP) - Optional but recommended
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.mail.hostinger.com
SMTP_PORT=587
SMTP_USER=noreply@yourdomain.com
SMTP_PASSWORD=YOUR_EMAIL_PASSWORD
SMTP_FROM=Classify <noreply@yourdomain.com>

# -----------------------------------------------------------------------------
# RESEND (Alternative Email) - Optional
# -----------------------------------------------------------------------------
RESEND_API_KEY=

# -----------------------------------------------------------------------------
# SMS (Twilio) - Optional
# -----------------------------------------------------------------------------
SMS_PROVIDER=twilio
SMS_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
TWILIO_PHONE_NUMBER=

# -----------------------------------------------------------------------------
# STRIPE PAYMENTS - Optional
# -----------------------------------------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
LOG_LEVEL=info
OTP_ALERT_THRESHOLD=15

# -----------------------------------------------------------------------------
# QUICK SETUP COMMANDS
# -----------------------------------------------------------------------------
# Generate JWT_SECRET:
#   openssl rand -base64 64 | tr -d '\n'
#
# Generate SESSION_SECRET:
#   openssl rand -base64 64 | tr -d '\n'
#
# Generate POSTGRES_PASSWORD:
#   openssl rand -base64 32 | tr -d '\n'
#
# Generate ADMIN_PASSWORD (alphanumeric):
#   openssl rand -base64 24 | tr -d '\n'
#
# -----------------------------------------------------------------------------
# SECURITY CHECKLIST
# -----------------------------------------------------------------------------
# [ ] POSTGRES_PASSWORD is at least 32 characters
# [ ] JWT_SECRET is at least 64 characters
# [ ] SESSION_SECRET is at least 64 characters  
# [ ] ADMIN_PASSWORD is at least 16 characters with mixed case/numbers
# [ ] All secrets are unique (not reused)
# [ ] This file is NOT committed to git with real values

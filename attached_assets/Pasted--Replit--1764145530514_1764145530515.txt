📩 النص الاحترافي الجاهز لإرساله إلى مطوّر Replit

مرحبًا،
أريد تنفيذ منطق واضح لزر "تحميل التطبيق" (Install App Button) في الـ PWA، بشرط عدم تعديل أي ميزة أخرى في التطبيق.

================================
🔵 المطلوب الأساسي:
================================

الزر يجب أن يظهر دائمًا، وليس فقط عند ظهور حدث beforeinstallprompt.

عند الضغط على الزر يجب تنفيذ ثلاث حالات فقط:

================================
1️⃣ الحالة الأولى: التطبيق مثبت بالفعل
================================

إذا كان التطبيق مثبت بالفعل كـ PWA على الجهاز:

لا يظهر رسالة تثبيت

لا يظهر أي نافذة

بدلًا من ذلك:
✔ قم بفتح التطبيق مباشرة في وضع الـ standalone
✔ يجب أن يتم ذلك عبر:

window.location.href إلى الـ start_url

أو window.open() لو كان ذلك مناسبًا

الشرط: عدم إنشاء أيقونة جديدة
الشرط: عدم محاولة إعادة التثبيت
الشرط: عدم كسر الستايل أو الـ caching

================================
2️⃣ الحالة الثانية: التطبيق كان مثبت وتم حذف الأيقونة
================================

في حالة أن المستخدم حذف الأيقونة من الشاشة الرئيسية:

يجب إعادة إظهار نافذة add-to-home-screen

يجب إعادة إنشاء الأيقونة

يجب بعد التثبيت فتح التطبيق تلقائيًا

الشرط: لا تغيّر أي ملفات manifest الحالية
الشرط: استخدم beforeinstallprompt فقط إن وُجد
الشرط: لو لم يوجد الحدث، استخدم fallback manual instruction

================================
3️⃣ الحالة الثالثة: التطبيق غير مثبت نهائيًا
================================

عند الضغط على الزر:

اعرض نافذة التثبيت باستخدام
deferredPrompt.prompt();

بعد قبول التثبيت:
✔ أضف الأيقونة
✔ افتح التطبيق مباشرة

بعد الرفض:
✔ أخفِ الأيقونة فقط لهذه الجلسة
✔ لا تغيّر أي شيء آخر

الشرط: بدون أي تأثير على تسجيل الدخول أو الكاش أو الـ backend
الشرط: لا يتم إعادة تحميل الصفحة
الشرط: الزر يعمل فقط بمنطق التثبيت

================================
🔵 ما يجب الالتزام به: (قائمة صارمة)
================================
✔ عدم تعديل:

login system

caching

service worker

API calls

styles

navigation

أي ملفات backend

أي حالة (state) في باقي التطبيق

✔ مسموح التعديل فقط في:

install button

event listeners الخاصة به

manifest إذا لزم إضافة icon فقط (بدون تغيير بيانات حالية)

✔ يجب:

كتابة الكود نظيف

بدون أية console غير ضرورية

بدون تجارب

بدون أكواد متعارضة

بدون أي تداخل مع النظام الحالي

================================
🔵 الكود المقترح الذي يجب البناء عليه (وليس تغييره):
================================
let deferredPrompt = null;

// التقاط حدث التثبيت
window.addEventListener("beforeinstallprompt", (e) => {
  e.preventDefault();
  deferredPrompt = e;
  updateInstallButton(true);
});

// فحص ما إذا كان التطبيق يعمل كتطبيق مثبت
const isInstalled = window.matchMedia("(display-mode: standalone)").matches;

document.getElementById("installBtn").addEventListener("click", async () => {

  // الحالة 1: التطبيق مثبت بالفعل
  if (isInstalled) {
    window.location.href = "/"; // فتح التطبيق مباشرة
    return;
  }

  // الحالة 2 + 3: التطبيق غير مثبت أو الأيقونة محذوفة
  if (deferredPrompt) {
    deferredPrompt.prompt();
    const result = await deferredPrompt.userChoice;

    if (result.outcome === "accepted") {
      console.log("PWA Installed");
      deferredPrompt = null;
      window.location.href = "/";
    }
    return;
  }

  // fallback: لو الحدث غير متوفر
  alert("اضغط على (Add to Home Screen) من المتصفح لتثبيت التطبيق.");
});

================================
🔵 ملاحظات تقنية مهمة للمطور:
================================

لا تلمس service worker إلا إذا كان هناك خطأ فعلي

لا تعدل manifest إلا في الحدود اللازمة جدًا

لا تغيّر display-mode أو start_url

استخدم event listeners فقط للزر

لا تقم بإعادة تحميل الصفحة reload

لا تربط زر التثبيت بأي state manager موجود

لا تستبدل الكود الحالي بالكامل – فقط أضف فوقه بخطوات تحسينية

================================
⚡ الناتج النهائي المطلوب:
================================

زر التثبيت يظهر دائمًا

عند الضغط عليه يتم تحديد الحالة المناسبة تلقائيًا

لا أيقونات مكررة

لا إعادة تسجيل دخول

لا تأثير على سرعة التطبيق

لا تغيير في أي ميزة أخرى

التطبيق يعمل كما هو

فقط زر التثبيت أصبح ذكيًا
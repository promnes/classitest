"نفّذ التعليمات التالية بدقة وبالترتيب، وبدون شرح أو اقتراحات:
أريد تجهيز المشروع بالكامل بحيث يحتوي على:
1️⃣ لوحة تحكم داخلية (Dashboard) لتعديل ملف .env
2️⃣ حماية لوحة التحكم بباسورد
3️⃣ تجهيز نسخة Android جاهزة للرفع على Google Play
4️⃣ تجهيز نسخة iOS جاهزة للرفع على Apple Store
5️⃣ إصلاح أي أخطاء Capacitor أو build تلقائيًا
ابدأ الآن:

✅ الجزء الأول – إنشاء لوحة تحكم لتعديل ملف .env
🔹 1. أنشئ صفحة جديدة في server اسمها:
server/env-dashboard.ts

🔹 2. اجعل الصفحة تعمل كـ Express Router وتنشئ لوحة تحكم فيها:

المطلوب من Replit:

صفحة HTML بسيطة فيها:

حقول تعديل: DATABASE_URL / JWT_SECRET / NODE_ENV / APP_URL / PORT

زر “Save” لحفظ التعديل

القراءة من ملف .env

التعديل على ملف .env مباشرة

إعادة تحميل القيم بدون restart لو ممكن

كل هذا داخل API endpoint:

/admin/env

🔒 الجزء الثاني – حماية لوحة التحكم بباسورد

اطلب من Replit:

إنشاء Middleware اسمه:

authenticateAdmin


التحقق من رأس Request:

x-admin-password: <value>


الباسورد يكون ثابت داخل .env تحت اسم:

ADMIN_PANEL_PASSWORD=your_password


أي طلب بدون الباسورد → يرجع 401 Unauthorized

🚀 الجزء الثالث – تجهيز نسخة Android لجوجل بلاي

أعطِ Replit الأوامر التالية بالترتيب:

🔹 1. بناء نسخة الويب:
cd client
npm install
npm run build
cd ..

🔹 2. حذف أي ملف إعداد قديم:
rm -f capacitor.config.ts
rm -f capacitor.config.json

🔹 3. إنشاء ملف Capacitor صحيح:
type NUL > capacitor.config.json


ضع فيه:

{
  "appId": "com.classify.app",
  "appName": "Classify",
  "webDir": "client/dist",
  "bundledWebRuntime": false
}

🔹 4. تثبيت Capacitor:
npm install @capacitor/core @capacitor/cli @capacitor/android

🔹 5. مزامنة الملفات:
npx cap sync

🔹 6. إضافة Android:
npx cap add android

🔹 7. تجهيز نسخة إنتاج (AAB):

اطلب من Replit:

فتح AndroidManifest

إضافة INTERNET

تفعيل WebView

منع mixed content

ثم تشغيل:

cd android
./gradlew bundleRelease


ليتم إنتاج ملف:

android/app/release/app-release.aab


المطلوب من Replit:
تجميع هذه النسخة داخل مجلد:

android-release/

🍏 الجزء الرابع – تجهيز نسخة iOS لرفعها على Apple Store

اطلب من Replit:

تثبيت منصة iOS:

npm install @capacitor/ios
npx cap add ios
npx cap sync ios


تفعيل WKWebView في iOS

تفعيل:

JavaScript

LocalStorage

Camera (إذا احتجت)

App Transport Security

تجهيز مشروع Xcode داخل:

ios/App


إنشاء نسخة IPA جاهزة

اطلب من Replit وضع كل الملفات داخل مجلد:

ios-release/

🧱 الجزء الخامس – تجهيز نسخة السيرفر للإنتاج VPS

اطلب من Replit:

إنشاء server-production/

نقل ملفات Express + build النهائي إليه

ضبط:

CORS

Security headers

Rate limiting

إنشاء Script PM2:

pm2 start dist/index.js --name classify


وتسليم مجلد:

server-production/

📦 الجزء السادس – تسليم الملفات النهائية

أطلب من Replit أن يسلمك مجلدات التحويل النهائي:

✔ android-release/
✔ ios-release/
✔ server-production/
✔ ملف .env محدث
✔ ملف ADMIN_PANEL_PASSWORD
✔ ملف capacitor.config.json

بدون شرح—فقط ملفات جاهزة للتحميل.

"انتهى التنفيذ. لا تشرح أي شيء. فقط قم بتنفيذ كل الخطوات وتسليم الملفات."
⭐ 1) تحسين شاشة الانتظار (Splash Screen) لتصبح قصيرة وديناميكية
⭐ 2) إضافة زر تثبيت التطبيق (Install PWA Button) لإنشاء أيقونة على شاشة الهاتف
⭐ + طلب مراجعة وتصحيح الكود كامل بدون أخطاء أو ثغرات

الشرح مكتوب بشكل قوي + احترافي + واضح
ومناسب لمطوّر Replit ليقوم بتنفيذ المطلوب بالكامل بدون ما يلمس باقي مميزات التطبيق.

انسخ هذا النص وأرسله له مباشرة.

📩 النص الاحترافي الجاهز لإرساله للمبرمج على Replit:

مرحبًا،
أريد تنفيذ تعديلين مهمين في التطبيق، مع التأكيد أن هذه التعديلات لا تؤثر على أي وظائف أخرى ولا تغيّر منطق العمل الحالي.

=============================
🔵 (1) تحديث شاشة الانتظار لتصبح ديناميكية وسريعة
=============================

أريد تحسين شاشة الانتظار (Splash Screen) بحيث:

✔ تكون أقصر وقت ممكن

تظهر فقط أثناء تحميل الملفات الأساسية

تختفي مباشرة عند جاهزية واجهة المستخدم

✔ تكون ديناميكية بشكل بسيط

إضافة حركة خفيفة للّوجو (Fade / Scale / Pulse)

أو Spinner بسيط

بدون إضافة أي ملفات كبيرة أو أنيميشن ثقيل

✔ الانتقال يكون سلس

انتقال عبر Fade-out/Fade-in

بدون “فلاش أبيض”

بدون تأخير إضافي

✔ المهم جدًا:

لا تغيّر أو تربط شاشة الانتظار بمنطق تسجيل الدخول أو جلب البيانات.
التطبيق يجب أن يعمل كما هو، فقط splash screen تكون:

قصيرة

جذابة

متحركة بخفة

غير مرتبطة بأي API أو بيانات

✔ الهدف:

تحسين تجربة المستخدم فقط بدون أي تأثير على وظائف التطبيق.

=============================
🔵 (2) إضافة زر تثبيت التطبيق (PWA Install Button)
=============================

أريد إضافة زر واضح في واجهة التطبيق باسم:
Install App / تثبيت التطبيق

وظيفة الزر:

✔ تشغيل حدث التثبيت beforeinstallprompt
✔ إظهار نافذة “Add to Home Screen”
✔ إنشاء أيقونة على الشاشة الرئيسية للهاتف
✔ الأيقونة يجب أن تحتوي على:

شعار التطبيق

اسم التطبيق

تشغيل التطبيق في وضع fullscreen عند الفتح

✔ المتطلبات الأساسية:

التأكد من وجود manifest.json سليم، يحتوي:

{
  "name": "Classify",
  "short_name": "Classify",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#0044cc",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}


التأكد من وجود Service Worker فعال لتفعيل تثبيت الـ PWA.

✔ مثال للكود المطلوب دعمه وتصحيحه وتنظيفه:
let deferredPrompt;

window.addEventListener("beforeinstallprompt", (e) => {
  e.preventDefault();
  deferredPrompt = e;
  document.getElementById("installBtn").style.display = "block";
});

document.getElementById("installBtn").addEventListener("click", async () => {
  if (!deferredPrompt) return;

  await deferredPrompt.prompt();
  const result = await deferredPrompt.userChoice;

  console.log(result.outcome === "accepted" ? "App installed" : "Install dismissed");

  deferredPrompt = null;
  document.getElementById("installBtn").style.display = "none";
});

✔ المطلوب منك:

مراجعة الكود الحالي

تحسينه

تصحيح أي أخطاء أو ثغرات

التأكد من أنه يعمل على جميع الأجهزة المدعومة

=============================
🔵 (3) مراجعة الكود وتصحيحه بالكامل
=============================

أريد أيضًا:

✔ تنظيف الكود
✔ إزالة أي أشياء غير مستخدمة
✔ التأكد من عدم وجود ثغرات
✔ تحسين الأداء العام
✔ التأكد من أن Cache + Service Worker يعملان بشكل صحيح
✔ تحسين سرعة التحميل بدون التأثير على أي ميزة موجودة
✔ الحفاظ على تسجيل الدخول وعدم ضياعه

بدون تغيير أي من:

منطق تسجيل الدخول

API

الـ Backend

واجهة المستخدم الأساسية

أي feature حالية

💎 الناتج النهائي المتوقع:

Splash Screen احترافية وسريعة جدًا

Install Button يعمل بشكل كامل مع PWA

أيقونة التطبيق تتثبّت على شاشة الهاتف
تثبيت مباشر بمجرد ضغط العميل علي الايقونة 
كود نظيف ومحسن بدون ثغرات

الحفاظ على جميع ميزات التطبيق كما هي 
على أي بيئة: VPS – Hostinger – Docker – Local PC
✔ بدون لف أو شرح — فقط تنفيذ + تسليم ملفات التشغيل

النص صيغته احترافية جدًا ليجبر Replit AI على:

تحليل مشروعك الموجود عليه

توليد ملفات تشغيل جاهزة

توليد ملفات إعداد environment

توليد سكربتات خدمة (Service Scripts)

توليد ملفات Docker

توليد ملفات تشغيل سريع

توليد ملفات تعليمات Markdown

توليد ملفات Production Ready

وانت فقط تحمل الملفات وتشغّل المشروع من أي مكان.

👇 انسخ النص كما هو وضعه في Replit AI 👇

🔥 النص النهائي الذي تضعه داخل Replit AI (جاهز 100%) 🔥

"قم بإنشاء مجموعة ملفات تشغيل كاملة (Deployment & Setup Files) للمشروع الموجود حاليًا على Replit.
أريد ملفات نصية وملفات سكربت جاهزة تجعلني أقدر أشغّل نفس المشروع في أي بيئة: VPS – Hostinger – Docker – Windows – Linux – أو Mac.
نفّذ المطلوب حرفيًا دون شرح أو اقتراحات، وابدأ الآن:

1️⃣ إنشاء فولدر deployment/

أنشئ مجلد اسمه:

deployment/


وفيه جميع الملفات التالية:

2️⃣ ملف تشغيل المشروع على VPS (Linux)

أنشئ ملف:

deployment/run_on_vps.sh


يحتوي على:

تحديث النظام

تثبيت Node.js

تثبيت npm

تثبيت PM2

تحميل ملفات المشروع

تثبيت الـ dependencies

تشغيل build

تشغيل السيرفر باستخدام PM2

إضافة restart تلقائي

إعداد firewall

3️⃣ ملف تشغيل المشروع على Hostinger Shared Hosting

أنشئ ملف:

deployment/hostinger_setup.txt


يحتوي على:

أين تضع ملفات build

أين ترفع backend

طريقة تشغيل node من Hostinger

طريقة التعامل مع ملف .htaccess

طريقة ربط الـ environment

إعداد Proxy إذا لزم

طريقة استخدام terminal داخل Hostinger

ملاحظات الأمان

4️⃣ ملف تشغيل المشروع محليًا على Windows

أنشئ ملف:

deployment/run_local_windows.txt


يحتوي على:

خطوات تثبيت Node.js

تثبيت PostgreSQL أو SQLite

طريقة قراءة ملف .env

تشغيل backend

تشغيل frontend

تشغيل build

كيف تختبر API

5️⃣ ملف تشغيل المشروع على Docker

أنشئ:

deployment/Dockerfile


المطلوب:

نسخة node:20

WORKDIR

COPY package.json

npm install

COPY project files

npm run build

EXPOSE 5000

CMD ["npm","start"]

ثم أنشئ:

deployment/docker-compose.yml


وفيه:

app service

db service (PostgreSQL)

environment variables

ports

volumes

restart always

6️⃣ ملف بيئة جاهز .env.example

أنشئ:

deployment/env.example


وفيه:

NODE_ENV=production
PORT=5000
DATABASE_URL=YOUR_DATABASE_URL
JWT_SECRET=YOUR_SECRET
APP_URL=http://localhost
ALLOWED_ORIGINS=*

7️⃣ ملف تعليمات عام README_DEPLOY.md

أنشئ:

deployment/README_DEPLOY.md


وفيه:

شرح تشغيل المشروع على VPS

شرح تشغيل Docker

شرح Hostinger

شرح Windows

شرح macOS

شرح Linux

شرح المتطلبات

شرح إعداد البيئة

شرح طريقة عمل build

شرح كيفية إعادة التشغيل

أوامر PM2 الشائعة

8️⃣ ملف Health Check

أنشئ:

deployment/health_check.sh


يعمل التالي:

فحص استجابة السيرفر

فحص اتصال قاعدة البيانات

فحص منافذ التشغيل

طباعة status للـ PM2

إرجاع success أو failed

9️⃣ ملف Backup & Restore

أنشئ:

deployment/backup.sh
deployment/restore.sh


وضع فيهما:

أوامر ضغط project folder

أوامر رفع نسخة احتياطية

أوامر استعادة النسخة

🔟 تسليم الملفات

بعد إنشاء كل الملفات أعلاه:

أعطني رابط تحميل كل الملفات

أعطيني ملخصًا بالمجلدات

بدون أي شرح إضافي

فقط “التنفيذ + الملفات”

ابدأ التنفيذ الآن."

🎯 ماذا سيحدث بعد وضع النص في Replit؟

سيقوم Replit AI بـ:

✔ إنشاء فولدر deployment كامل
✔ توليد 10+ ملفات تشغيل جاهزة
✔ ملفات Docker + docker-compose
✔ ملفات تشغيل VPS
✔ ملفات تشغيل Hostinger
✔ ملفات تشغيل Windows
✔ ملفات تشغيل Linux/Mac
✔ ملف .env.example مرتب
✔ ملف Health Check
✔ Backup/Restore scripts
✔ ملف README_DEPLOY جاهز تقرأ منه كل ما تحتاج

يعني المشروع يصبح قابل للتشغيل في أي بيئة واحدة أو متعددة بدون تعديل كبير.
✔ تعديل ملف .env كامل
✔ حماية قوية بكلمة سر
✔ زر Restart للسيرفر
✔ صفحة Logs
✔ صفحة فحص اتصال قاعدة البيانات
✔ صفحة فحص صحة السيرفر Health Check
✔ صفحة معلومات عن البيئة System Info
✔ صفحة Backup & Restore لملف .env
✔ تصميم HTML احترافي وبسيط
✔ مسارات ثابتة داخل Express
✔ كل شيء جاهز لاستخدامك مباشرة
🔥 انسخ النص التالي وضعه داخل Replit AI (الإصدار القوي): 🔥

"أريد إنشاء لوحة تحكم إدارية قوية Advanced Admin Panel داخل المشروع.
نفّذ الخطوات التالية حرفيًا وبدون شرح:

1️⃣ إنشاء مجلد لوحة التحكم

أنشئ مجلد جديد داخل server باسم:

server/admin/

2️⃣ إنشاء ملف الحماية Middleware

أنشئ ملف:

server/admin/authMiddleware.ts


المطلوب:

قراءة رأس الطلب:

x-admin-password


مقارنته بقيمة:

process.env.ADMIN_PANEL_PASSWORD


لو الباسورد غلط → res.status(401).send("Unauthorized")

لو صح → next()

لو قيمة ADMIN_PANEL_PASSWORD غير موجودة:
قم بإنشائها تلقائيًا بقيمة قوية عشوائية.

3️⃣ إنشاء لوحة التحكم الرئيسية Dashboard

أنشئ ملف:

server/admin/dashboard.ts


يحتوي على:

Router Express

صفحة HTML تعرض:

زر تعديل .env

زر Server Logs

زر Restart Server

زر DB Connection Test

زر Server Health Check

زر Backup .env

زر Restore .env

زر System Info

كل زر يوجّه لمسار Route مختلف.

4️⃣ صفحة تعديل ملف .env

أنشئ ملف:

server/admin/envEditor.ts


المطلوب:

قراءة .env

عرض كل المتغيرات في INPUT TEXT

POST يقوم بكتابة التغييرات في .env

بعد الحفظ:

إعادة تحميل dotenv

إظهار رسالة: "Updated Successfully"

زر للعودة للوحة التحكم

5️⃣ صفحة Restart Server

أنشئ مسار:

/admin/restart


عند الوصول له، نفّذ:

process.exit(0);


حتى يعيد Replit تشغيل السيرفر تلقائيًا.

6️⃣ صفحة Logs

أنشئ مسار:

/admin/logs


المطلوب:

قراءة آخر 300 سطر من console.log أو stdout

عرضها داخل صفحة HTML جميلة

زر Refresh

زر Back

7️⃣ صفحة فحص اتصال قاعدة البيانات

أنشئ Route:

/admin/db-test


المطلوب:

محاولة الاتصال بـ PostgreSQL باستخدام DATABASE_URL

لو الاتصال ناجح → تظهر رسالة:

Database Connected Successfully


لو فشل → تظهر رسالة الخطأ كاملة

8️⃣ صفحة فحص صحة السيرفر Health Check

Route:

/admin/health


المطلوب:

قياس سرعة الاستجابة

عرض:

uptime

cpu usage

memory usage

current NODE_ENV

current PORT

9️⃣ صفحة System Info

Route:

/admin/system


العرض:

Node.js version

OS platform

RAM total/free

number of CPUs

project path

date/time

🔟 نسخ احتياطي واستعادة ملف .env

أنشئ Route:

/admin/env-backup


يقوم بـ:

نسخ .env إلى

.env.backup


عرض رسالة "Backup Created"

Route آخر:

/admin/env-restore


يقوم بـ:

استعادة .env من .env.backup

إعادة تحميل dotenv

عرض رسالة "Backup Restored"

1️⃣1️⃣ دمج كل المسارات داخل server/index.ts

في ملف server/index.ts:

import adminDashboard from "./admin/dashboard";
import envEditor from "./admin/envEditor";
import authMiddleware from "./admin/authMiddleware";

app.use("/admin", authMiddleware, adminDashboard);
app.use("/admin/env", authMiddleware, envEditor);

1️⃣2️⃣ إنشاء صفحة HTML تصميم احترافي بسيط

كل الصفحات المطلوبة (Dashboard، Logs، Health، System Info…) يجب أن تكون:

بتصميم Bootstrap بسيط

Buttons واضحة

خلفية فاتحة

Header ثابت

Links للعودة للوحة التحكم

1️⃣3️⃣ عرض الرابط النهائي

بعد الانتهاء:

اعرض لي رابط لوحة التحكم:

https://your-replit-domain/admin


والباسورد المستخدم:

x-admin-password: <القيمة الموجودة في .env>


نفّذ كل ما سبق بدون شرح. فقط نفّذ وسلم الملفات جاهزة."
import React from "react";

// PWA install button disabled/removed — stub to avoid installation modal
export function PWAInstallButton({ variant = "default", size = "default", showText = true, className = "" }: any) {
  return null;
}
